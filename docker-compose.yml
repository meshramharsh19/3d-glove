version: "3.8"

services:
  backend:
    build: ./backend
    container_name: glove_backend
    ports:
      - "4000:4000"

  frontend:
    build: ./frontend
    container_name: glove_frontend
    ports:
      - "4001:80"
    depends_on:
      - backend
